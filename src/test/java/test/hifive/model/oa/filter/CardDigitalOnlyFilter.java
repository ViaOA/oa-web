// Generated by OABuilder
package test.hifive.model.oa.filter;

import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;

import test.hifive.model.oa.*;

import java.util.*;

@OAClass(useDataSource=false, localOnly=true)
@OAClassFilter(name = "DigitalOnly", displayName = "Digital Only", hasInputParams = false)
public class CardDigitalOnlyFilter extends OAObject implements CustomHubFilter<Card> {
    private static final long serialVersionUID = 1L;


    public static final String PPCode = ":DigitalOnly()";
    private Hub<Card> hubMaster;
    private Hub<Card> hub;
    private HubFilter<Card> filter;
    private boolean bAllHubs;

    public CardDigitalOnlyFilter(Hub<Card> hub) {
        this(true, null, hub);
    }
    public CardDigitalOnlyFilter(Hub<Card> hubMaster, Hub<Card> hub) {
        this(false, hubMaster, hub);
    }
    public CardDigitalOnlyFilter(boolean bAllHubs, Hub<Card> hubMaster, Hub<Card> hubFiltered) {
        this.hubMaster = hubMaster;
        this.hub = hubFiltered;
        if (hubMaster == null) this.hubMaster = new Hub<Card>(Card.class);
        this.bAllHubs = bAllHubs;
        getHubFilter(); // create filter
    }


    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }
    public void refresh() {
        if (filter != null) getHubFilter().refresh();
    }

    @Override
    public HubFilter<Card> getHubFilter() {
        if (filter == null) {
            filter = createHubFilter(hubMaster, hub, bAllHubs);
        }
        return filter;
    }
    protected HubFilter<Card> createHubFilter(final Hub<Card> hubMaster, Hub<Card> hub, boolean bAllHubs) {
        HubFilter<Card> filter = new HubFilter<Card>(hubMaster, hub) {
            @Override
            public boolean isUsed(Card card) {
                return CardDigitalOnlyFilter.this.isUsed(card);
            }
        };
        filter.addDependentProperty(Card.P_DigitalCard);
 
        if (!bAllHubs) return filter;
        // need to listen to all Card
        OAObjectCacheHubAdder hubCacheAdder = new OAObjectCacheHubAdder(hubMaster);
        return filter;
    }

    public boolean isUsed(Card card) {
        // digitalCard
        return card.getDigitalCard();
    }
    
}

// Generated by OABuilder
package test.xice.tsac3.model.oa.search;

import com.viaoa.annotation.*;
import com.viaoa.datasource.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;

import test.xice.tsac3.model.oa.*;

import com.viaoa.filter.OAQueryFilter;

@OAClass(useDataSource=false, localOnly=true)
public class MRADServerSearch extends OAObject {
    private static final long serialVersionUID = 1L;



    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<MRADServer> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (!OAString.isEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<MRADServer> f = new OAQueryFilter<MRADServer>(MRADServer.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<MRADServer> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<MRADServer> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<MRADServer> getSelect() {
        String sql = "";
        String sortOrder = "";
        Object[] args = new Object[0];

        if (!OAString.isEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<MRADServer> sel = new OASelect<MRADServer>(MRADServer.class, sql, args, sortOrder);
        sel.setDataSourceFilter(this.getDataSourceFilter());
        sel.setFilter(this.getCustomFilter());
        return sel;
    }

    private OAFilter<MRADServer> filterDataSourceFilter;
    public OAFilter<MRADServer> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<MRADServer>() {
            @Override
            public boolean isUsed(MRADServer mradServer) {
                return MRADServerSearch.this.isUsedForDataSourceFilter(mradServer);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<MRADServer> filterCustomFilter;
    public OAFilter<MRADServer> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<MRADServer>() {
            @Override
            public boolean isUsed(MRADServer mradServer) {
                boolean b = MRADServerSearch.this.isUsedForCustomFilter(mradServer);
                if (b && filterExtraWhere != null) b = filterExtraWhere.isUsed(mradServer);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(MRADServer mradServer) {
        return true;
    }
    public boolean isUsedForCustomFilter(MRADServer mradServer) {
        return true;
    }
}

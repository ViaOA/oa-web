// Generated by OABuilder
package test.xice.tsac3.model.oa.search;

import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.util.filter.OAQueryFilter;

import test.xice.tsac3.model.oa.*;

import com.viaoa.ds.*;

import java.io.*;

@OAClass(useDataSource=false, localOnly=true)
public class ServerFileSearch extends OAObject {
    private static final long serialVersionUID = 1L;



    public void reset() {
    }

    public boolean isDataEntered() {
        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<ServerFile> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (!OAString.isEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<ServerFile> f = new OAQueryFilter<ServerFile>(ServerFile.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<ServerFile> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<ServerFile> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<ServerFile> getSelect() {
        String sql = "";
        String sortOrder = "";
        Object[] args = new Object[0];

        if (!OAString.isEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<ServerFile> sel = new OASelect<ServerFile>(ServerFile.class, sql, args, sortOrder);
        sel.setDataSourceFilter(this.getDataSourceFilter());
        sel.setFilter(this.getCustomFilter());
        return sel;
    }

    private OAFilter<ServerFile> filterDataSourceFilter;
    public OAFilter<ServerFile> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<ServerFile>() {
            @Override
            public boolean isUsed(ServerFile serverFile) {
                return ServerFileSearch.this.isUsedForDataSourceFilter(serverFile);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<ServerFile> filterCustomFilter;
    public OAFilter<ServerFile> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<ServerFile>() {
            @Override
            public boolean isUsed(ServerFile serverFile) {
                boolean b = ServerFileSearch.this.isUsedForCustomFilter(serverFile);
                if (b && filterExtraWhere != null) b = filterExtraWhere.isUsed(serverFile);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(ServerFile serverFile) {
        return true;
    }
    public boolean isUsedForCustomFilter(ServerFile serverFile) {
        return true;
    }
}

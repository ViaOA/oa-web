// Generated by OABuilder
package test.xice.tsam.model.oa.search;

import java.util.logging.*;

import test.xice.tsam.model.oa.ApplicationStatus;
import test.xice.tsam.model.oa.search.ApplicationStatusSearch;
import com.viaoa.annotation.*;
import com.viaoa.object.*;
import com.viaoa.hub.*;
import com.viaoa.util.*;
import com.viaoa.ds.*;
import com.viaoa.util.filter.OAQueryFilter;

import test.xice.tsam.model.oa.*;
import test.xice.tsam.model.oa.propertypath.*;

@OAClass(useDataSource=false, localOnly=true)
public class ApplicationStatusSearch extends OAObject {
    private static final long serialVersionUID = 1L;
    private static Logger LOG = Logger.getLogger(ApplicationStatusSearch.class.getName());
    public static final String P_Name = "Name";
    public static final String P_NameUseNull = "NameUseNull";
    public static final String P_NameUseNotNull = "NameUseNotNull";
    public static final String P_Type = "Type";
    public static final String P_Type2 = "Type2";

    protected String name;
    protected boolean nameUseNull;
    protected boolean nameUseNotNull;
    protected int type;
    protected int type2;

    public String getName() {
        return name;
    }
    public void setName(String newValue) {
        fireBeforePropertyChange(P_Name, this.name, newValue);
        String old = name;
        this.name = newValue;
        firePropertyChange(P_Name, old, this.name);
    }
    
      
    public boolean getNameUseNull() {
        return nameUseNull;
    }
    public void setNameUseNull(boolean newValue) {
        boolean old = this.nameUseNull;
        this.nameUseNull = newValue;
        firePropertyChange(P_NameUseNull, old, this.nameUseNull);
    }
    public boolean getNameUseNotNull() {
        return nameUseNotNull;
    }
    public void setNameUseNotNull(boolean newValue) {
        boolean old = this.nameUseNotNull;
        this.nameUseNotNull = newValue;
        firePropertyChange(P_NameUseNotNull, old, this.nameUseNotNull);
    }

    public int getType() {
        return type;
    }
    public void setType(int newValue) {
        fireBeforePropertyChange(P_Type, this.type, newValue);
        int old = type;
        this.type = newValue;
        firePropertyChange(P_Type, old, this.type);
        if (isLoading()) return;
        if (type > type2) setType2(this.type);
    } 
    public int getType2() {
        return type2;
    }
    public void setType2(int newValue) {
        fireBeforePropertyChange(P_Type2, this.type2, newValue);
        int old = type2;
        this.type2 = newValue;
        firePropertyChange(P_Type2, old, this.type2);
        if (isLoading()) return;
        if (type > type2) setType(this.type2);
    }

    public void reset() {
        setName(null);
        setNameUseNull(false);
        setNameUseNotNull(false);
        setType(0);
        setNull(P_Type);
        setType2(0);
        setNull(P_Type2);
    }

    public boolean isDataEntered() {
        if (getName() != null) return true;
        if (getNameUseNull()) return true;if (getNameUseNotNull()) return true;
        if (getNameUseNull()) return true;
        if (getNameUseNotNull()) return true;

        return false;
    }

    protected String extraWhere;
    protected Object[] extraWhereParams;
    protected OAFilter<ApplicationStatus> filterExtraWhere;

    public void setExtraWhere(String s, Object ... args) {
        this.extraWhere = s;
        this.extraWhereParams = args;
        if (!OAString.isEmpty(s) && getExtraWhereFilter() == null) {
            OAFilter<ApplicationStatus> f = new OAQueryFilter<ApplicationStatus>(ApplicationStatus.class, s, args);
            setExtraWhereFilter(f);
        }
    }
    public void setExtraWhereFilter(OAFilter<ApplicationStatus> filter) {
        this.filterExtraWhere = filter;
    }
    public OAFilter<ApplicationStatus> getExtraWhereFilter() {
        return this.filterExtraWhere;
    }

    public OASelect<ApplicationStatus> getSelect() {
        String sql = "";
        String sortOrder = ApplicationStatus.P_Type;
        Object[] args = new Object[0];
        if (nameUseNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ApplicationStatus.P_Name + " = null";
        }
        else if (nameUseNotNull) {
            if (sql.length() > 0) sql += " AND ";
            sql += ApplicationStatus.P_Name + " != null";
        }
        else if (!OAString.isEmpty(this.name)) {
            if (sql.length() > 0) sql += " AND ";
            String value = name.replace("*", "%");
            if (!value.endsWith("%")) value += "%";
            if (value.indexOf("%") >= 0) {
                value = value.toUpperCase();
                sql += ApplicationStatus.P_Name + " LIKE ?";
            }
            else {
                sql += ApplicationStatus.P_Name + " = ?";
            }
            args = OAArray.add(Object.class, args, value);
        }
        if (!isNull(P_Type)) {
            if (sql.length() > 0) sql += " AND ";
            if (!isNull(P_Type2) && type != type2) {
                sql += ApplicationStatus.P_Type + " >= ?";
                args = OAArray.add(Object.class, args, getType());
                sql += " AND " + ApplicationStatus.P_Type + " <= ?";
                args = OAArray.add(Object.class, args, getType2());
            }
            else {
                sql += ApplicationStatus.P_Type + " = ?";
                args = OAArray.add(Object.class, args, getType());
            }
        }

        if (!OAString.isEmpty(extraWhere)) {
            if (sql.length() > 0) sql = "(" + sql + ") AND ";
            sql += extraWhere;
            args = OAArray.add(Object.class, args, extraWhereParams);
        }

        OASelect<ApplicationStatus> sel = new OASelect<ApplicationStatus>(ApplicationStatus.class, sql, args, sortOrder);
        sel.setDataSourceFilter(this.getDataSourceFilter());
        sel.setFilter(this.getCustomFilter());
        return sel;
    }

    private OAFilter<ApplicationStatus> filterDataSourceFilter;
    public OAFilter<ApplicationStatus> getDataSourceFilter() {
        if (filterDataSourceFilter != null) return filterDataSourceFilter;
        filterDataSourceFilter = new OAFilter<ApplicationStatus>() {
            @Override
            public boolean isUsed(ApplicationStatus applicationStatus) {
                return ApplicationStatusSearch.this.isUsedForDataSourceFilter(applicationStatus);
            }
        };
        return filterDataSourceFilter;
    }
    
    private OAFilter<ApplicationStatus> filterCustomFilter;
    public OAFilter<ApplicationStatus> getCustomFilter() {
        if (filterCustomFilter != null) return filterCustomFilter;
        filterCustomFilter = new OAFilter<ApplicationStatus>() {
            @Override
            public boolean isUsed(ApplicationStatus applicationStatus) {
                boolean b = ApplicationStatusSearch.this.isUsedForCustomFilter(applicationStatus);
                if (b && filterExtraWhere != null) b = filterExtraWhere.isUsed(applicationStatus);
                return b;
            }
        };
        return filterCustomFilter;
    }
    
    public boolean isUsedForDataSourceFilter(ApplicationStatus applicationStatus) {
        if (nameUseNull) {
            if (!OACompare.isEmpty(applicationStatus.getName())) return false;
        }
        else if (nameUseNotNull) {
            if (OACompare.isEmpty(applicationStatus.getName())) return false;
        }
        else if (name != null) {
            String s = getName();
            if (s != null && s.indexOf('*') < 0 && s.indexOf('%') < 0) s += '*';
            if (!OACompare.isLike(applicationStatus.getName(), s)) return false;
        }
        if (!isNull(P_Type2)) {
            if (!OACompare.isEqualOrBetween(applicationStatus.getType(), type, type2)) return false;
        }
        return true;
    }
    public boolean isUsedForCustomFilter(ApplicationStatus applicationStatus) {
        return true;
    }
}
